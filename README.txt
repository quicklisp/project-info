This project is split off from quicklisp-dist code used to make
release announcements.

The goal is to make use of it in quicklisp-controller for generating
more system metadata for compilation failure reports, with e.g. links
to project and system websites.

